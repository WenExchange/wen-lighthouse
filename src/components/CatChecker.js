import React from "react";

const CatChecker = ({ closePopup, phases }) => {
  return (
    <div
      className={`w-[80%] mt-[30px] flex flex-col text-[14px] font-thin`}
    ></div>
  );
};

export default CatChecker;
